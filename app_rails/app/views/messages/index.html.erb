<h3> Seleccione un usuario para mandar un nuevo mensaje </h3>
<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.full_name, messages_path(id: user.id) %></li>
  <% end %>
</ul>
<% if @reciever %>
  <%= render partial: "chat", locals: {reciever: @reciever} %>
<% end %>